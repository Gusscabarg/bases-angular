import {  ListDbzComponent } from './list.component';

describe('ListComponent', () => {
  let component: ListDbzComponent;

  beforeEach(() => {
    component = new ListDbzComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
